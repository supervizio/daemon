# Rust toolchain configuration for probe library
# This ensures consistent builds across development and CI environments

[toolchain]
channel = "1.93.0"
components = ["rustfmt", "clippy"]
targets = [
    "aarch64-apple-darwin",
    "aarch64-unknown-linux-gnu",
    "aarch64-unknown-linux-musl",
    "x86_64-apple-darwin",
    "x86_64-unknown-linux-gnu",
    "x86_64-unknown-linux-musl",
]
